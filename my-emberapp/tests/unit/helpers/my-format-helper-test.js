
import { myFormatHelper } from 'my-emberapp/helpers/my-format-helper';
import { module, test } from 'qunit';

module('Unit | Helper | my format helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = myFormatHelper([42]);
  assert.ok(result);
});

